---
const posts = Object.values(
  import.meta.glob("@/pages/posts/*.md", { eager: true })
);
---

<p class="text-2xl font-bold">Post Archive:</p>
<ul>
  {
    posts.map((post: any) => (
      <li class="text-lg">
        <a href={post.url}>{post.frontmatter.title}</a>
      </li>
    ))
  }
</ul>
